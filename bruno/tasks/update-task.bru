meta {
  name: Update Task
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/projects/{{projectId}}/tasks/{{taskId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "IN_PROGRESS",
    "priority": "MEDIUM"
  }
}
